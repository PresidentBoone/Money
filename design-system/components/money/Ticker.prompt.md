"Last payment received" live readout — mono pill with a pulsing LIVE dot and green amount.

```jsx
<Ticker amount="+$49.00" source="acme.co" ago="2 min ago" />
```

Props: `amount` (formatted), `source`, `ago`, `live` (toggles the dot + label). Drop it in the header next to the total.
