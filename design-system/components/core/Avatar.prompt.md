Circular co-founder face with the money-glow ring — the avatar that rides the road.

```jsx
<Avatar src="assets/cofounder-avatar.gif" size={72} ring="green" />
<Avatar src={faceUrl} size={48} ring="gold" />
```

Props: `src` (still or GIF), `size` (px diameter), `ring` (`green | gold | none`), `glow` (toggle the outer glow). Falls back to a "?" tile when `src` is empty.
